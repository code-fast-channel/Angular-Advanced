export const errMsg = {
    required: "{controlName} is required",
    email: "Enter a valid email",
    minlength: "should be minimum of {requiredLength} characters",
    maxlength: "should be maximum of {requiredLength} characters",
}
